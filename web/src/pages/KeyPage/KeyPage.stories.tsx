import type { Meta, StoryObj } from '@storybook/react'

import KeyPage from './KeyPage'

const meta: Meta<typeof KeyPage> = {
  component: KeyPage,
}

export default meta

type Story = StoryObj<typeof KeyPage>

export const Primary: Story = {}
